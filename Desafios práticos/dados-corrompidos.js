// PASSO A PASSO
// 1. olhar cada um dos caracteres
// 2. ver se pode o caractere em questao
// 3. se for permitido, escrevo
// 4. se nao for permitido, ignoro e olho proximo
// 5. quando acabarem os caracteres, eh soh exibir o que eu escrevi

// function solucao(stringCorrompida) {

//     let dadoPurificado = '';
//     for (let caractere of stringCorrompida) {
//         if (caractere === '!' || caractere === '@' || caractere === '#' || caractere === '$' || caractere === '%' || caractere === '&' || caractere === '*' || caractere === '(' || caractere === ')' || caractere === '.');
//     } else {
//         dadoPurificado += caractere;
//     }
//     console.log(dadoPurificado);
// }
// function processData(input) {
//     solucao(input);
// }


let dadoPurificado = '';

for (let caractere of stringCorrompida) {
    if (caractere === '!' || caractere === '@' || caractere === '#' || caractere === '$' || caractere === '%' || caractere === '&' || caractere === '*' || caractere === '(' || caractere === ')' || caractere === '.');
} else {

}